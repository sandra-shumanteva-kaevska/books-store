import { createMuiTheme } from '@material-ui/core/styles'

export const myTheme = createMuiTheme({
    typography: {
        fontFamily: [
            'Redressed',
            'cursive'
        ].join(','),
    }
})